<?php

require_once __DIR__ . '/vendor/autoload.php';
require_once __DIR__ . '/bootstrap/start.php';
use MkyCore\Console\Command;


if(php_sapi_name() === 'cli'){
    $command = new Command();
    $command->run($argv);
}